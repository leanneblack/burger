<div class="col-5 mx-auto">
  <h2 class="addBurger">Your Choice of HAMBURGER</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="ca">Burger Name:</label>
      <input type="text" id="ca" name="burgerName">
    </div>
</div>

<div class="col-5 mx-auto">
  <h2 class="addBurger">YOUR BURGER ?</h2>
  <div class="form-group inputText">
    {{!-- <label for="devoured">YOUR BURGER ?</label><br> --}}
    <input class="inputText" type="radio" name="devoured" value="1" checked> Already Devoured!<br>
    <input type="radio" class="inputText" name="devoured" value="0"> Ready for muh Belly!
  </div>

  <button class="btn btn-secondary btn-lg btn-block addbtn" type="submit">Add Your Burger</button>
</div>
</form>
</div>
</div>


<div class="container main">
  <div class="row">
    <div class="col-sm-5 mx-auto">
      <h2 class="innerTitle">Burgers Eaten!</h2>

      <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burger-block devoured=true}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="col-sm-5 mx-auto">
      <h2 class="innerTitle">burgers to devoure!</h2>

      <ul>
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burger-block devoured=false}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>